import React from 'react';
import { View, Text } from 'react-native';
import { getMetricMetaInfo } from '../utils/helper';

export default () => {
    return(
        <View>
            <Text>Add Entry</Text>
        </View>
    )
}